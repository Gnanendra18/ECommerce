package com.incrediblepath.ecommerce.application

import android.app.Application

class ECommerceApplication: Application() {

    override fun onCreate() {
        super.onCreate()
        //do some default loading required for app
    }
}